ter.class
warning: Cannot merge binary files: server_code/target/classes/com/cl/utils/Vali
datorUtils.class (HEAD vs. 31df6ed6 (使用户预约成功后立刻发送所有后续提醒,而非分
时段发送。同时判断用户接收道状态,通知发送失败需要有系统记录和重试机制,管理员可以
在后台对系统记录进行查看与处理。))
Auto-merging server_code/target/classes/com/cl/utils/ValidatorUtils.class
CONFLICT (add/add): Merge conflict in server_code/target/classes/com/cl/utils/Va
lidatorUtils.class
error: could not apply 31df6ed6... 使用户预约成功后立刻发送所有后续提醒,而非分时
段发送。同时判断用户接收道状态,通知发送失败需要有系统记录和重试机制,管理员可以在
后台对系统记录进行查看与处理。
hint: Resolve all conflicts manually, mark them as resolved with
hint: "git add/rm <conflicted_files>", then run "git rebase --continue".
hint: You can instead skip this commit: run "git rebase --skip".
hint: To abort and get back to the state before "git rebase", run "git rebase --
abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Could not apply 31df6ed6... # 使用户预约成功后立刻发送所有后续提醒,而非分时段发
送。同时判断用户接收道状态,通知发送失败需要有系统记录和重试机制,管理员可以在后台
对系统记录进行查看与处理。
